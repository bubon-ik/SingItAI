# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Node
node_modules/

# Local virtual environments
.venv/
venv/
env/
payment-executor/.venv/

# Secrets and local runtime config
# Ledger key ring ciphertext. The encrypted master key is still the master
# key: anyone who has it and WALLET_PASS has every managed wallet.
*.enc
.env
*.env
!*.env.example
payment-executor/.env
sign402-gateway/.bitrefill-api-key
.env.wallet-*
!.env.wallet-*.example

# Demo runtime artifacts
.demo-logs/
.ledger-live/
.graph-live/
# Runtime data retained at its historical path; includes SQLite sidecar files.
/demo-dashboard/

# Generated presentation scratch workspace
outputs/
output/
tmp/
SingIt-backgrounds.zip
SingIt-presentation-assets/

# Generated x402 survey exports; keep handwritten notes and patches visible.
/out/*.csv
/out/x402-services-live.md

# Local brainstorming sessions and preview server state
/.superpowers/

# Local tooling and duplicated/scratch workspaces
.tools/
tools/*
# …except the Ledger approval signer, which is part of the product rather than
# scratch: the local client signs a gateway operation on the owner's device.
!tools/ledger-approve/
x402HackBerlin/
.worktrees/

# Draft writing artifacts
Sign402 X Longform Post.md

# Local editor files
.vscode/
.idea/
/.codex/

# …but not its installed dependencies.
tools/ledger-approve/node_modules/
